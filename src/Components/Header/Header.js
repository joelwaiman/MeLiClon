import React from "react"
import { NavLink, Link} from "react-router-dom"

const Header = ()=>{
    return(
        <header>
            <h1>Hello World</h1>
        </header>
    )
}

export default Header;